# 更新日志

> 项目：`life`  
> 记录日期：2026-03-24

---

## 本次已完成工作

### 1) 项目梳理与说明文档完善
- 读取并梳理了核心文件：
  - `project.godot`
  - `scenes/` 下场景
  - `scripts/` 下核心系统脚本
  - `data/events.json`
- 重写了 `README.md`，补充了：
  - 游戏核心玩法结构
  - 属性/事件/NPC/微信系统说明
  - 游戏流程与结局逻辑
  - 项目目录说明

### 2) 游戏主界面开始“代码 + 可视化”混合重构（重点）
你提出希望 UI 同时使用代码与可视化，已完成 `Game` 主界面的分阶段迁移：

#### 阶段 A：场景结构落地（`scenes/Game.tscn`）
- 将 `Game.tscn` 从简单结构扩展为完整界面层级：
  - 顶部状态栏：`StatusBar`
  - 时间控制栏：`TimeControlBar`
    - `PauseBtn`
    - `Speed1xBtn` / `Speed2xBtn` / `Speed4xBtn`
    - `PhoneBtn`
    - `SpeedLabel` / `DateLabel`
  - 主体左右分栏：`MainHBox`
    - 左侧 `LeftPanel`：`EventText`、`ChoicesContainer`、`NextButton`
    - 右侧 `RightPanel`：属性区与标签区

#### 阶段 B：脚本绑定改造（`scripts/Game.gd`）
- 由“动态创建 UI”为主，改为“绑定场景节点 + 代码控制逻辑”：
  - 新增场景节点 `@onready` 绑定
  - 新增 `_bind_scene_nodes()`：绑定按钮、进度条、标签、信号
  - 保留游戏逻辑（推进天数、事件触发、属性变化、存档、结局）
- 删除了不再需要的动态 UI 构建依赖：
  - 不再通过旧方式构建状态栏/时间栏/主布局
- `update_ui()` 改为直接使用场景节点（不再依赖 meta 缓存）

#### 阶段 C：样式迁移（让可视化更可控）
- 把一部分固定样式迁移到 `Game.tscn`，便于在编辑器里直接改：
  - `StatusBar` 字体样式
  - `SpeedLabel`、`DateLabel`、`InfoHeader`、`TagsLabel`
  - 6项属性数值标签颜色/字号
- 新增并应用了场景内 `StyleBoxFlat` 子资源：
  - `StatusBar`（normal / hover）
  - `EventText` 背景
  - `RightPanel` 背景
- 相应地精简了 `Game.gd` 中重复的样式代码。

### 3) 代码清理
- 清理了迁移后不再使用的变量/空函数（如旧状态展开相关残留）。
- 每一步修改后已做语法与诊断检查，当前关键文件无 lints 报错。

---

## 当前项目结构（重点模块）

```text
life/
├── scenes/
│   ├── main_menu.tscn     # 主菜单场景（当前仍以脚本动态构建为主）
│   └── Game.tscn          # 游戏主场景（已迁移为可视化主导布局）
├── scripts/
│   ├── MainMenu.gd        # 主菜单、开场、读档流程
│   ├── Game.gd            # 游戏核心逻辑 + 场景节点绑定 + 动态UI刷新
│   ├── SaveManager.gd     # 存档系统（3槽位）
│   ├── NamePool.gd        # NPC姓名生成
│   ├── RelationshipManager.gd  # NPC好感与关系等级
│   ├── WechatSystem.gd    # 微信消息模板与回复效果
│   ├── PhoneSystem.gd     # 手机界面逻辑（微信/通讯录等）
│   └── AudioManager.gd    # 菜单/游戏BGM管理
├── data/
│   └── events.json        # 主线与日常事件库
├── audio/
│   ├── menu_bgm.mp3
│   └── game_bgm.mp3
├── project.godot
├── README.md              # 已完善说明
└── 更新日志.md            # 本文件
```

---

## 当前 UI 架构状态（你最关心）

### 已经实现
- **布局结构**：主要放在 `Game.tscn`（可视化编辑）
- **业务逻辑**：在 `Game.gd`（事件、属性、时间推进、结局等）
- **数据绑定**：`Game.gd` 中通过 `@onready` 指向场景节点
- **部分样式**：已转入 `Game.tscn`

### 仍在代码中的样式（正常）
- 进度条 fill/background 的动态设置（依赖属性色）
- 选择按钮、弹窗按钮等运行时创建控件的样式

这意味着现在已经进入你要的模式：
**“可视化控制结构 + 代码控制逻辑与动态部分”**。

---

## 下阶段可选（未执行）
- 将 `PhoneSystem` 用独立 `Phone.tscn` 承载，复用同样的“可视化 + 脚本”模式。
- 对 `MainMenu` 做同样迁移，统一全项目 UI 规范。

---

## 本次补充收尾（UI）
- `Game.tscn`：完成 `TimeControlBar` 按钮样式场景化（`PauseBtn` / `Speed1xBtn` / `Speed2xBtn` / `Speed4xBtn` / `PhoneBtn`）。
- `Game.gd`：删除对应时间按钮样式函数调用与 `_style_time_btn()`，避免场景与代码重复定义。
- `main_menu.tscn`：补齐主菜单基础可视化结构（背景、中心主框、标题、副标题、主按钮区、版本号、遮罩）。
- `main_menu.tscn`：新增并场景化 `SavePanel` 与 `CharPanel` 基础层级（存档列表容器、角色输入区、按钮区）。
- `MainMenu.gd`：由 `_build_ui()` 动态搭建切换为 `_init_main_scene_bindings()` 节点绑定；主菜单按钮样式函数改为 `_style_menu_btn(btn, color)`。
- `MainMenu.gd`：新增 `_bind_save_panel()` / `_bind_char_panel()`，改为绑定场景中的存档与角色面板节点。
- `main_menu.tscn`：新增 `LoadPage` 与 `LoadCardTemplate` 作为读档页模板节点。
- `MainMenu.gd`：`_show_load_page()` 改为复用场景模板生成卡片，避免整页完全动态拼装。
- `MainMenu.gd`：清理迁移后无用分支与旧函数（移除 `is_loading` 相关分支、移除 `_back_to_main_menu`）。
- 修复路径：返回主菜单改为 `res://scenes/main_menu.tscn`（与实际文件名一致）。
- 当前状态：
  - `Game` 已基本完成混合 UI 收尾。
  - `MainMenu` 主菜单主视图已场景化，弹出面板（存档/角色创建/读档页）仍为动态构建。

---

## 备注
- 本更新日志用于快速了解本轮改动与当前项目形态。
- 若后续继续重构，会在本文件追加新日期记录。
