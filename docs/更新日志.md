# 更新日志

## 2026-04-01

### 核心代码修复（阶段化）

- `Game.gd`
  - 修复晨间信息阶段未立即刷新 UI 的问题
  - 修复学年/学期与阶段计算（按 `day_in_year`）
  - 调整学期结算触发点（`120` / `310`）
  - 重做 GPA 与学分累计逻辑，增加学期记录与预警计数
  - 接入 `events.json` 与 `flavor_texts.json` 事件触发链路
  - 增加事件展示、选项处理、标签处理
  - 增强行动按钮文本：效果预览 + 费用显示
  - 补全行动条件分支：关系、天赋、能力等
  - 补全存档：Wechat/RelationshipManager/NamePool 子系统序列化

- `PlayerInfoPanel.gd`
  - 增加本地 `BACKGROUNDS` 映射，避免跨脚本常量依赖崩溃
  - 修复剧情进度在事件为空时的安全返回
  - 修复待回复计数逻辑，改为基于聊天记录读取

- `RelationshipManager.gd`
  - 注册恋爱线角色模板（林知意、苏小晚、陈雨桐、周一然、沈映霜）
  - 设定陈雨桐开局已认识

- `LoveModule.gd`
  - 初遇触发从“每天扫描”改为“行动后匹配触发”
  - 增加 `_check_first_meet_on_action()` 并在行动执行时调用

- `PhoneSystem.gd`
  - 增加手机遮罩层并支持点击遮罩关闭
  - 打开/关闭手机时同步遮罩显示状态

### 文档整理

- 更新根目录 `README.md`（GitHub 首页展示版）
- 重写并保留 docs 三份核心文档：
  - `Godot结构与UI结构说明.md`
  - `项目全部代码汇总.md`
  - `更新日志.md`
- 清理 docs 中其他非保留文档
